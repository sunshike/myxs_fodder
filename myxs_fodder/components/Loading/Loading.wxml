<view class="cover">
  <view class="container">
    <view class="lds-dual-ring"></view>
    <view wx:if='{{!isDownload}}'>{{progress || 0}}%</view>
    <view wx:if='{{isDownload}}'>{{title}}</view>
  </view>
</view>